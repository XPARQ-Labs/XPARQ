# Developer tools

XPARQ exposes native Layer-1 asset operations through the wallet and node RPC.
See [ASSETS.md](ASSETS.md) for the current asset workflow.

Useful validation commands:

```bash
cargo check --workspace --all-targets --locked
cargo test --workspace --locked
```
